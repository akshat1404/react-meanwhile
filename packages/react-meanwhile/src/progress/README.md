# progress

This is where producer-driven loader types (scanner, csv-parser,
long-running-query status, etc.) will live once they are built. Unlike the
reveal loaders in `../reveal/`, they wrap no existing content to measure and
take an explicit progress prop instead. None exist yet — this directory only
marks the intended architecture and is not a stub implementation.
